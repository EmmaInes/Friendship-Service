// Google OAuth Client ID — set this to your Google Cloud Console client ID
// This is safe to commit; client IDs are not secret.
export const GOOGLE_CLIENT_ID = import.meta.env.VITE_GOOGLE_CLIENT_ID || 'YOUR_GOOGLE_CLIENT_ID';
